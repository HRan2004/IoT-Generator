import Property, {From} from "./core/property";
import Parser from "./core/parser";

let DSM: any = {} // Double State Manager

/* GENERATE DEVICE INIT */

try {
  init().then(r => {
    console.log('Init Success.')
    try {
      main().then(r => {})
    } catch (e) {
      console.error('Run Error.')
      console.error(e)
    }
  })
} catch (e) {
  console.error('Init Error.')
  console.error(e)
}

// Init function
async function init(): Promise<void> {
  // Init DSM states
  /* GENERATE DSM INIT */
  // Init set function
  /* GENERATE INIT SET FUNCTION */
  // Init properties
  /* GENERATE INIT PROPERTIES */
  // Init remote receive
  /* GENERATE INIT REMOTE RECEIVE */
}

// Main function
async function main(): Promise<void> {
  // Edges property bind
  /* GENERATE EDGES PROPERTY BIND */
}

